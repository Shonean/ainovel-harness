# Webnovel Writer - Python Dependencies
# Python >= 3.10 required

# 核心依赖
aiohttp>=3.8.0          # 异步 HTTP 客户端（API 调用）
filelock>=3.0.0         # 文件锁（状态文件并发控制）
pydantic>=2.0.0         # Schema 校验
numpy>=1.24.0           # 向量运算
faiss-cpu>=1.7.4        # FAISS 向量索引（FAISS 不可用时自动降级 numpy）

# 可选依赖（开发/测试）
pytest>=7.0.0           # 单元测试
pytest-cov>=4.1.0       # 覆盖率统计
pytest-asyncio>=0.23.0  # 异步测试支持
pytest-timeout>=2.3.0   # 测试超时保护
